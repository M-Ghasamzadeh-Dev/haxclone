// =========================================================
// config.js — همه‌ی مقادیر ثابت و قابل‌تنظیم بازی اینجاست.
// اگه خواستی فیزیک/سایز زمین/قدرت شوت رو عوض کنی، فقط همین فایل رو دستکاری کن.
// =========================================================

export const CONFIG = {
  FIELD: {
    W: 900,
    H: 520,
    GOAL_WIDTH: 140,   // ارتفاع دهانه‌ی دروازه
    GOAL_DEPTH: 30,    // عمق تور به بیرون از زمین
    WALL_BOUNCE: 0.75, // ضریب برگشت از دیوار (۰=نمی‌پره، ۱=کامل برمی‌گرده)
  },

  PLAYER: {
    R: 17,
    ACCEL: 0.55,
    MAX_SPEED: 6.2,
    FRICTION: 0.985,   // اصطکاک حرکت بازیکن (نزدیک ۱ = لغزنده‌تر)
  },

  BALL: {
    R: 11,
    FREE_FRICTION: 0.997, // اصطکاک توپِ آزاد (خیلی نزدیک ۱ یعنی توپ خیلی دیر کند میشه -> کنترل سخت)
    WALL_BOUNCE: 0.85,
  },

  DRIBBLE: {
    // وقتی توپ به بازیکن می‌چسبه (دریبل)، جلوی بازیکن با این فاصله قرار می‌گیره
    STICK_OFFSET: 32,
    // بعد از شوت، توپ تا این مدت (ms) نمی‌تونه دوباره به همون بازیکن بچسبه
    RESTICK_COOLDOWN_MS: 350,
    // شعاعی که توپ آزاد باید داخلش باشه تا به بازیکن بچسبه
    PICKUP_RADIUS_EXTRA: 6,
  },

  KICK: {
    POWER: 19, // سرعت اولیه‌ی توپ بعد از شوت (خیلی زیاده -> کنترلش سخته)
  },

  MATCH_DEFAULTS: {
    TEAM_SIZE: 3,     // ظرفیت هر تیم (پیش‌فرض)
    DURATION_MIN: 5,  // مدت بازی به دقیقه، 0 = نامحدود
    GOAL_LIMIT: 3,    // امتیاز برد، 0 = نامحدود
  },

  NETWORK: {
    TICK_MS: 1000 / 40, // نرخ ارسال state از هاست به بقیه (۴۰ بار در ثانیه)
  },
};
